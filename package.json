{
  "name": "moj_elements",
  "version": "0.1.0",
  "description": "CSS and JavaScript for MOJ Digital Serivces which are not already available in GOV.UK Elements",
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "bower": "^1.3.12",
    "grunt": "~0.4.5",
    "grunt-contrib-jasmine": "^0.8.1",
    "grunt-contrib-jshint": "~0.10.0",
    "grunt-contrib-watch": "~0.6.1",
    "jasmine-jquery": "^2.0.6",
    "jquery-browser": "^1.10.2-1"
  },
  "scripts": {
    "postinstall": "./node_modules/bower/bin/bower install",
    "test": "node_modules/.bin/grunt jasmine"
  }
}
